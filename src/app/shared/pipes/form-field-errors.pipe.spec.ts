import { FormFieldErrorsPipe } from './form-field-errors.pipe';

describe('FormFieldErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormFieldErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
